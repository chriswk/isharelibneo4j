%p#notice= notice
.hero-unit
  %p
    %b Title:
    = @movie.title
  %p
    %b Release year:
    = @movie.year

  %h3
    People involved
  %table.table-striped
    %thead
      %tr
        %th Job
        %th Name
        %th Role
    %tbody
      - @movie.people_rels.each do |role|
        %tr
          %td= role.job
          %td= link_to role.start_node.name, person_url(role.start_node.id)
          %td= role.character
  .pull-right
    .well
      In Country: @movie.countries.first.name
  .well
    = link_to 'Edit', edit_movie_path(@movie)
    \|
    = link_to 'Back', movies_path
